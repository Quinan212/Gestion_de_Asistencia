import 'package:flutter/material.dart';

class ComboSelector extends StatelessWidget {
  const ComboSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50);
  }
}
