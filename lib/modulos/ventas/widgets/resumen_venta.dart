import 'package:flutter/material.dart';

class ResumenVenta extends StatelessWidget {
  const ResumenVenta({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('Total: \$0.00'),
      ],
    );
  }
}
