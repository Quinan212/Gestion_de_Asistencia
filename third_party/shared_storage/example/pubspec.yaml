name: shared_storage_example
description: Demonstrates how to use the shared_storage plugin.

publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  fl_toast: ^3.1.0
  flutter:
    sdk: flutter
  lint: ^1.8.2
  shared_storage:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
